# Data Layout

- `raw/` stores provider source files such as Metrica open tracking data.
- `processed/` stores validated and transformed derivatives created by the pipeline.

Both folders are intended for DVC-managed artifacts and are excluded from git.
